# ExecPlans

复杂任务先写 ExecPlan，再实现。

## 什么时候必须写

满足任一条件就落盘：

- 涉及多个文件或模块
- 涉及重构、状态流、持久化、网络或并发
- 预计步骤超过 5 步
- 实现路径不够稳，写着容易失控

单文件、小修复可直接做。

## 写到哪里

- 统一写到：`docs/execplans/<任务名>.md`
- 模板来源：`.agent/PLANS.md`

每次复杂任务只维护一个计划文件。

## 执行顺序

1. 先读 `.agent/PLANS.md`
2. 写 `docs/execplans/<任务名>.md`
3. 计划落盘后，再开始实现
4. 实现过程中持续更新同一份计划
5. 完成后在同一份计划里补结果

## 硬约束

- 计划未落盘前，不开始复杂任务实现
- 不再拆分 `before` / `after`
- 一个任务只维护一个计划文件
- 偏离计划时，先记决策，再继续实现
- 未来写 plan 默认遵循：精简、美观、真实路径、用户结果优先

## 交付规则

- 每次修改后必须构建 app，并用构建产物替换 `/Applications/MangaGlass.app`。
- 每次代码或 UI 行为修改都要增加版本号：小改动 `+0.0.1`，大改动 `+0.1`。

## Git 提交规则

- 用户要求提交且未指定语言时，commit message 的 `feat:` 内容使用中文。
- 每次修改版本号后必须提交 commit，commit message 使用中文 `feat:`。
